{
  "name": "moonraker-dashboard-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Terminal dashboard for monitoring a Klipper/Moonraker 3D printer",
  "scripts": {
    "dev": "pnpm --filter dashboard dev",
    "start": "pnpm --filter dashboard start",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@10.11.0",
  "dependencies": {
    "dashboard": "^0.0.1",
    "supports-terminal-graphics": "^0.1.0",
    "terminal-image": "^4.3.0"
  }
}
